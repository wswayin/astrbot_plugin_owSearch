# Resources

本目录用于存放 `overstats` 运行和绘制过程中使用的资源文件。

此处 `readme.md` 预留用于撰写以下规范：

1. 资源文件分类规则。
2. 命名规范与目录分层约定。
3. 可提交资源与不可提交资源的界限。
4. 大文件、生成文件和缓存文件的处理方式。
5. 图片、字体、模板等素材的来源说明要求。

后续新增资源时，应先确认是否符合这里定义的资源管理规范。
